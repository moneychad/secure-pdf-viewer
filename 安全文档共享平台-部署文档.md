
---

## 五、当前测试环境信息（192.168.100.107）

### 5.1 架构
```
浏览器 → Nginx (:80) → FastAPI 后端 (:8000)
```

### 5.2 服务管理

| 组件 | 服务名 | 状态 |
|------|--------|------|
| FastAPI 后端 | secure-pdf-viewer.service | systemd 管理 |
| Nginx | nginx.service | systemd 管理 |

```bash
# 后端服务
sudo systemctl status secure-pdf-viewer    # 查看状态
sudo systemctl restart secure-pdf-viewer   # 重启
sudo systemctl stop secure-pdf-viewer      # 停止
sudo systemctl start secure-pdf-viewer     # 启动
sudo journalctl -u secure-pdf-viewer -f    # 实时日志
sudo journalctl -u secure-pdf-viewer --since '1 hour ago'  # 最近1小时日志

# Nginx
sudo systemctl status nginx
sudo systemctl restart nginx
sudo nginx -t                              # 测试配置
```

### 5.3 关键路径

| 路径 | 说明 |
|------|------|
| /opt/secure-pdf-viewer/ | 项目根目录 |
| /opt/secure-pdf-viewer/backend/main.py | 后端代码 |
| /opt/secure-pdf-viewer/backend/venv/ | Python 虚拟环境 |
| /opt/secure-pdf-viewer/backend/database.db | SQLite 数据库 |
| /opt/secure-pdf-viewer/backend/uploads/ | 上传的 PDF 文件 |
| /opt/secure-pdf-viewer/frontend/ | 前端文件 |
| /etc/systemd/system/secure-pdf-viewer.service | 服务配置 |
| /etc/nginx/sites-available/secure-pdf-viewer.conf | Nginx 配置 |

### 5.4 服务配置详情

- **运行用户**：hope
- **工作目录**：/opt/secure-pdf-viewer/backend
- **Python 环境**：/opt/secure-pdf-viewer/backend/venv/bin/python
- **SECRET_KEY**：存储在 systemd 服务文件中
- **默认管理员**：admin / admin123（首次部署后必须修改）
- **开机自启**：已启用
- **自动重启**：是（Restart=always, RestartSec=5）

### 5.5 Git 版本管理

```bash
cd /opt/secure-pdf-viewer
git log --oneline          # 查看提交历史
git status                 # 查看工作区状态
git diff                   # 查看未提交的修改
```

### 5.6 数据库操作

```bash
cd /opt/secure-pdf-viewer/backend
sqlite3 database.db

# 常用查询
.tables                          # 列出所有表
SELECT * FROM users;             # 查看用户
SELECT * FROM access_logs LIMIT 10;  # 查看访问日志
SELECT * FROM audit_logs LIMIT 10;   # 查看审计日志
SELECT * FROM user_groups;       # 查看用户组
```

### 5.7 常见问题排查

| 问题 | 排查方法 |
|------|----------|
| 服务无法启动 | sudo journalctl -u secure-pdf-viewer --since '5 minutes ago' |
| 页面空白 | 浏览器 F12 查看 Console 错误 |
| 403 错误 | 检查文件权限 ls -la frontend/js/ |
| 500 错误 | 查看后端日志中的 Traceback |
| 登录失败 | 检查 Cookie 是否正确设置，确认 SECRET_KEY 一致 |
| JS 文件加载失败 | 检查文件权限是否为 644 |
