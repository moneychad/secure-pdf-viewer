# CHANGELOG

## v7.58 - 2026-09-12

### 安全（登录链接双因素）
- **登录链接不再免密**: 按领导要求，访客打开登录链接后仍需输入该链接绑定账号的用户名和密码才能登录——链接（时效+可吊销）只负责"开门"，凭据证明身份
- 两段式接口：`POST /api/login-link` 仅带 token 时校验链接并返回绑定账号（前端显示表单，用户名预填）；带用户名+密码时校验 链接有效+账号匹配+密码正确 才签发会话
- 凭据校验沿用登录频率限制（防爆破）；会话有效期仍跟随链接

### 部署/运维
- 前端版本号：`v=7.58`

---

## v7.57 - 2026-09-12

### 性能（外网访问优化）
- **WebP 图片输出**: `/view` 接口按 Accept 头自动协商 WebP（q85），无 WebP 支持时回退 JPEG；实测单页 102KB(JPEG@150dpi) → 19.5KB(WebP@110dpi)，体积降约 80%；缓存键包含格式，渲染缓存机制不变
- **默认渲染 DPI 150→110**: 屏幕阅读清晰度足够，体积再降约45%；放大超过130%时自动升级 dpi=200 高清渲染（防抖400ms、保留缩放级别），缩回恢复
- **share.html**: fetch 补 Accept 头，分享页同样享受 WebP
- 背景：外网日志分析显示服务器/带宽正常（其他外网客户端 avg rt≈0.01s），个别客户端跨运营商链路极慢（76KB 图片 avg 16s），减体积是服务端最有效的优化

### 部署/运维
- 前端版本号：`v=7.57`

---

## v7.56 - 2026-09-12

### 优化
- **链接对外域名可配置**: 新增后端环境变量 `PUBLIC_BASE_URL`，生成/复制的登录链接与分享链接统一以该地址为基础；107 已配置为 `https://dr.auv-x.com`（外网平台），108 不配置（保持 `http://192.168.100.108` 内网形式，未配置时回退为当前访问来源）

### 部署/运维
- 前端版本号：`v=7.56`
- 外网域名已从 dataroom.auv-x.com 切换为 `https://dr.auv-x.com`（外层Nginx 110.19 配置+证书已更新）

---

## v7.55 - 2026-09-12

### 优化
- **登录页闪烁**: 登录页改为默认隐藏，新增启动加载页（会话校验期间显示）；通过登录链接进入的用户不再短暂看到用户名/密码输入框
- **协议弹窗空白**: 移除 `.agreement-modal-content` 的 `height:100vh`，弹窗高度自适应内容，按钮下方不再有大片空白
- **登录链接有效期**: 新增「其他（自定义天数）」选项，可手动输入 1~30 的整数，单位「天」明确显示在输入框右侧

### 部署/运维
- 前端版本号：`v=7.55`

---

## v7.54 - 2026-09-12

### 新增功能
- **平台登录链接（免密动态登录）**: 管理员在用户管理页为非管理员账号生成时效登录链接（默认24小时），访客打开 `/login.html?token=XXX` 免密以该账号身份进入平台，权限/水印/菜单全部跟随该账号；会话有效期跟随链接（链接到期会话同步失效）；可随时吊销；后端强制禁止为管理员账号生成
- **文档分享链接**: 文档行🔗生成单文档免登录查看链接（默认24小时，可选访问密码），外部人员通过 `/share.html?token=XXX` 逐页查看带水印图片（水印为 访客-token标识），每次翻页都重新校验时效/吊销状态，到期即失效；有效期内不限打开次数；页头「分享管理」可复制/吊销
- **密码复杂度强制**: ≥8位 + 数字 + 大小写字母 + 特殊字符；覆盖管理员创建用户、用户修改密码、管理员重置密码、分享链接访问密码（前后端双校验）

### 修复
- **删除用户接口500**: f-string 内 `user[username]` 漏引号的预存 bug

### 安全
- 分享/登录链接均为 32 位随机 token，不可猜测；全量审计（创建/使用/吊销写 audit_logs，外部访问写 access_logs 含 IP）
- 107/108 锁网：禁止主动访问内网其他服务器及外网（OUTPUT 链 = lo + ESTABLISHED,RELATED + 全 DROP，iptables-restore.service 开机自动恢复）

### 部署/运维
- 前端版本号：`v=7.54`
- 新增页面：`login.html`（登录链接落地页）、`share.html`（文档分享查看页）
- 新增文件：`js/share.js`、`js/loginlink.js`
- 后端：`render_page_jpeg()` 从 view_document 抽取共用；新增 `share_links`、`login_links` 两表

---

## v7.52+perf - 2026-09-11

### 修复
- **全屏竖图截断**: 全屏时 wrapper 改回 `overflow:auto` + 顶部对齐，竖向图片上半部分不再被裁，且可滚动/拖拽
- **表格内滚动**: `#main-page` 改为 flex column，修复目录行数多的时候主显示区被撑高、表格内无法纵向滚动的问题
- **目录树目录切换**: 正在浏览 PDF 时点击目录树目录，直接切换到目标目录列表，不再需要先点“返回列表”
- **全屏按钮状态**: 全屏后按钮变为“退出全屏”，点击可退出；退出后恢复“全屏”
- **目录树高亮**: 高亮当前正在浏览的文件或文件夹，并自动展开/滚动到对应位置

### 性能
- **PDF 渲染输出 JPEG**: `/api/documents/{id}/view` 从 PNG 改为 JPEG quality=88，并去掉 PNG `optimize=True`
- **渲染缓存**: 新增 `backend/cache/rendered` 最终 JPEG 缓存；缓存键包含文档/文件mtime/页码/dpi/MAX_RENDER_DIM/用户名/分钟级水印时间；TTL 2 小时，启动强制清理，运行中每小时最多清理一次
- **超大扫描页限速**: 新增 `MAX_RENDER_DIM=2200`，避免 dpi=150 把扫描件渲染成 5000x7000 导致首次加载和公网传输过慢；普通 A4 页面不受影响
- **水印时间精度**: 为让缓存可复用，水印时间从秒级调整为分钟级

### 部署/运维
- 前端版本号：`v=7.52`
- 107/108 已部署并验证；外层域名 Nginx 增加 keepalive/gzip_proxied/大响应 buffer/rt-urt 耗时日志

---

## v1.2.0 - 2026-08-26

### 新增功能
- **目录逐个上传**: 大目录上传从一次性打包改为逐个文件串行上传，解决HTTP 413问题
- **上传进度条**: 目录上传时显示实时进度条（正在上传 N/M 个 PDF 文件）
- **上传结果详情**: 上传完成后显示成功/失败数量，失败文件列出具体原因
- **新增单文件目录上传接口**: 后端 /api/documents/upload-directory-single

### 优化
- Nginx上传大小限制: 200M -> 1024M
- 单个文件上传失败不影响其他文件继续上传
- 单文件上传大小限制: 50MB -> 200MB（含普通上传、批量上传、逐个上传三个接口）
- 前端版本号 v6.0 -> v6.1

---

## v1.1.0 - 2026-08-20

### 新增功能
- **上传目录**: 支持上传整个文件夹(webkitdirectory)，自动创建子目录结构，非PDF文件静默忽略
- **批量移动支持目录**: 选中文档和目录后可批量移动到目标目录，含循环引用保护
- **目录数量显示**: 目录名后显示 `(N个目录, N个文件)` 格式

### 修复
- 修复目录上传路径丢失bug：保留完整webkitRelativePath（之前错误地去掉了第一层目录名）
- 修复目录创建缓存bug：缓存最终结果而非中间节点ID
- 修复目录删除bug：递归删除所有子目录和文档（之前只把当前目录文档移到根目录）
- 修复上传接口参数类型：relative_paths从Query改为Form
- 修复上传错误显示：JSON解析容错，避免[object Object]和HTML解析报错
- nginx上传大小限制：50M -> 200M

---

## v1.0.1 - 2026-08-20

### UI调整
- 登录页logo显示在用户名上方，居中卡片布局，底部copyright
- 主页面header左侧logo(40px PNG无损格式)
- 全局固定底部copyright: "联拓海洋版权所有©2024-2026 未经书面允许，不得泄露"
- CSS全局0.9x缩放比例（登录卡片342px）
- logo全部换为PNG无损格式，解决模糊问题

### 新增功能
- 水印增加第二行"联拓海洋内部资料，严禁外泄"(bold 22px)

---

## v1.0.0 - 2026-08-16

### 初始版本
- Canvas渲染PDF（防下载）
- 动态水印（用户名+时间+设备指纹）
- 设备指纹采集
- 访问日志（停留时间+文件路径+北京时间）
- 审计日志
- 目录管理（新建/重命名/删除）
- 文档管理（上传/删除/移动）
- 批量操作（删除/移动/权限）
- 搜索排序
- 用户管理（CRUD+编辑弹窗）
- 用户组管理（成员管理弹窗）
- 细粒度权限（用户/用户组 × 目录/文件）
- 权限继承
- 列搜索过滤
- Cookie认证(bcrypt+JWT HttpOnly)+10分钟超时
- 登录频率限制(5次/15分钟)
- Nginx反向代理+X-Accel-Redirect文件下载控制
